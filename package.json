{
  "name": "gnv-template",
  "version": "3.0.0",
  "description": "",
  "type": "module",
  "bin": {
    "gnv-template": "./dist/cli.cjs",
    "gnv-template-dev": "./exports/cli.js"
  },
  "main": "./dist/node.cjs",
  "exports": {
    "import": "./dist/node.mjs",
    "require": "./dist/node.cjs"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "scripts": {
    "build:dev": "yarn run:rollup && yarn run:webpack",
    "build:dist": "yarn run:gulp",
    "build": "yarn clean && yarn build:dev && yarn build:dist && yarn test",
    "clean": "rm -rf ./dist/* && rm -rf ./dev/*",
    "run:gulp": "yarn gulp -f .gulpfile.js",
    "run:rollup": "yarn rollup -c .rollup.config.js",
    "run:webpack": "yarn webpack --config .webpack.config.cjs",
    "test": "yarn mocha"
  },
  "devDependencies": {
    "babel-eslint": "^10.1.0",
    "chai": "^4.2.0",
    "commander": "^6.0.0",
    "eslint": "^7.6.0",
    "eslint-config-google": "^0.14.0",
    "eslint-plugin-jsdoc": "^30.2.2",
    "google-closure-compiler": "^20200719.0.0",
    "google-closure-library": "^20200719.0.0",
    "gulp": "^4.0.2",
    "gulp-cli": "^2.3.0",
    "mocha": "^8.1.1",
    "rollup": "^2.24.0",
    "rollup-plugin-export-default": "^1.4.0",
    "rollup-plugin-preserve-shebang": "^1.0.1",
    "shebang-loader": "^0.0.1",
    "webpack": "~4.44.1",
    "webpack-cli": "~3.3.12"
  }
}
